APP_NAME=SMSArea
APP_ENV=local
APP_KEY=base64:phs0VLYIL2kSDgUWBNG+29oSbfyjIKJqWiLjk/WmKFE=
APP_DEBUG=true
APP_URL=http://api.smsarea.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=smsarea
DB_USERNAME=postgres
DB_PASSWORD=a14e08df66311a0afc7f0b1b71dee3a1cd5fcf8c82c9031b

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
SMS_GATEWAY_WS_URL=https://totul-gateway-ws.fly.dev
SMS_GATEWAY_HTTP_URL=https://totul-gateway-ws.fly.dev
SMS_GATEWAY_PRESENCE_PREFIX=sms_gateway:presence:

# AI chat HTTP API (e.g. local /api/chat) — campaign inbound when ai_inbound_enabled and status active
AI_URL=http://127.0.0.1:11434
AI_MODEL=qwen2.5:7b
AI_TIMEOUT_SECONDS=120
# Fallback when a campaign has no per-campaign AI system prompt (see campaigns.ai_inbound_system_prompt).
AI_CAMPAIGN_INBOUND_SYSTEM_PROMPT=
# How many prior SMS rows (user+assistant turns) to send as history (4–200, default 48).
AI_CAMPAIGN_INBOUND_MAX_CONTEXT_MESSAGES=48
# Seconds to wait after the last inbound SMS before AI runs (2–90). Resets on each new inbound so bursts become one reply.
AI_CAMPAIGN_INBOUND_DEBOUNCE_SECONDS=10
# Human-like pause after AI text before SMS: base seconds + typing time from length (chars / rate), capped at max.
AI_CAMPAIGN_INBOUND_HUMAN_DELAY_SECONDS=2
# Typing speed for length scaling (chars/sec). Higher = shorter delay. 0 = base only, no scaling by length.
AI_CAMPAIGN_INBOUND_HUMAN_CHARS_PER_SECOND=12
# Max total human delay seconds (0 = disable human delay).
AI_CAMPAIGN_INBOUND_HUMAN_DELAY_MAX_SECONDS=30
# Extra instructions appended to every campaign AI system prompt. Set to empty to disable.
# AI_CAMPAIGN_INBOUND_GUARDRAILS=
